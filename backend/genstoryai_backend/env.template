# ollama
OPENAI_API_KEY=123566
OPENAI_BASE_URL="http://localhost:11434/v1"
OPENAI_MODEL="qwen3:4b"


# Database config
DATABASE_URL=sqlite:///./genstoryai.db  # Database connection string

# JWT config
SECRET_KEY=your-secret-key-change-this-in-production  # Secret key for JWT
ALGORITHM=HS256  # JWT algorithm
ACCESS_TOKEN_EXPIRE_MINUTES=120  # JWT token expiration in minutes

# Mail config
SMTP_SERVER=your-mail-smtp.com  # SMTP server address
SMTP_PORT=your-email-smtp-port  # SMTP server port (465 for SSL, 587 for TLS)
SMTP_USERNAME=notification@example.com  # SMTP login username (usually your email address)
SMTP_PASSWORD=your-email-password  # SMTP login password or app-specific password
MAIL_SIMULATE=true  # If true, simulate email sending (for development); set to false in production

# Mail sender info
SMTP_EMAIL_FROM_NAME=example  # Display name for sender
SMTP_EMAIL_FROM_ADDRESS=notification@example.com  # Sender email address

# Frontend config
FRONTEND_URL=http://localhost:5173  # Frontend base URL for email link

# Logging config
LOG_LEVEL=INFO  # Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE=genstoryai.log  # Log file path (leave empty for console only)